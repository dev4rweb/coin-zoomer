import React from 'react';
import Layout from "../components/Layout";

const ContactsPage = () => {
    return (
        <Layout>
            <h1>Contacts Page</h1>
        </Layout>
    );
};

export default ContactsPage;
